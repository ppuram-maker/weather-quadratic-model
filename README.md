# weather-quadratic-model
"Weather modeling using a quadratic equation solver. The project progresses through four stages: hard-coded inputs, keyboard input, single file input, and multiple file inputs. Demonstrates increasing complexity in input handling and program structure".
